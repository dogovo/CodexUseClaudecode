interface:
  display_name: "Codex Use Claude"
  short_description: "Run Claude Code as a tracked helper"
  default_prompt: "Use $codex-use-claude to ask Claude Code for a read-only review and collect the result."
policy:
  allow_implicit_invocation: true
